# 跨平台流媒体歌单导出工具 — Docker Compose 环境变量示例
# 复制为 .env 后按需修改；.env 已被 .gitignore / .dockerignore 忽略，绝不提交或进入镜像。
# docker compose 会自动读取工作目录下的 .env。

# 认证说明：无需再配置 ACCESS_TOKEN（已废弃；若 .env 中仍残留该变量，
# 服务端会忽略它并在启动日志中警告"检测到已废弃的 ACCESS_TOKEN，已忽略"）。
# 登录鉴权改由数据卷 /data/auth.json 管理：
#   - 首次启动自动创建默认账号 admin/admin，并在启动日志中提示尽快修改；
#   - 登录后可在界面中修改用户名与密码（修改成功后除当前会话外的全部会话失效）；
#   - 会话保存在 /data/sessions.json，重启后登录态保留；登录时可自选
#     12 小时 / 7 天 / 30 天 / 永久（10 年）后需重新登录。

# 宿主机端口（容器内固定监听 4319，右侧容器端口请勿修改）。
PORT=4319

# 宿主机数据目录，映射到容器内 /data（只读根文件系统下唯一持久化目录之一）。
# 账号（auth.json）与会话（sessions.json）也保存在该目录，请保证可写并定期备份。
# （非容器方式直接运行服务端时，该变量同时作为服务端读取账号/会话的目录；
#   不设置时服务端默认使用 /data，本地裸跑建议显式指向一个可写目录。）
DATA_DIR=./data

# 跨源白名单（ALLOWED_ORIGINS）：逗号分隔的完整 origin（协议+主机+端口，精确匹配）。
# - 默认服务 app：无需设置。compose 会按宿主映射端口自动传入
#   http://127.0.0.1:<PORT> 与 http://localhost:<PORT>（PORT 见上方）。
# - LAN 模式 app-lan：必须设置为局域网设备浏览器实际访问的完整 UI origin，
#   否则其他设备的浏览器请求会被 403 拒绝。示例（把 IP 换成你的宿主机地址）：
#   ALLOWED_ORIGINS=http://192.168.1.49:4319
# 不要填写 *：服务端会直接拒绝包含 * 或带路径/凭证的 origin，容器将无法启动。
#ALLOWED_ORIGINS=
