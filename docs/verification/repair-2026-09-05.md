# 修复轮验证记录:GLM-REPAIR-TASKS-2026-09-05 执行结果

日期:2026-09-05。
工作树:`D:/gpt/2026-09-02/apple-music-qq-utf-8-txt/.worktrees/core-netease-mvp`(分支 codex/core-netease-mvp,审查 HEAD 1d23c53,所有改动保持未提交)。

本记录区分:已有修复(任务单第 1 节列出的前一位代理未提交改动,原样保留)、此次修复(下文 01–09 及 Docker 实测阶段追加的 2 项容器专属修复)、未验证项(第 4 节)。本文不是"全部文件安全审计通过"的证明。

**Docker 实测阶段追加说明(晚于第 1 节的并行分工)**:在 192.168.1.49(SSH,用户 chatgpt,Docker 28.5.2/buildx 0.29.1/Compose v2.40.3,x86_64)完成容器实测时,发现并修复了 2 个静态检查无法暴露的容器专属缺陷(见第 2 节 03/04 项的"实测追加"),涉及 `pnpm-workspace.yaml` 与 `Dockerfile` 两个文件的追加改动,并在改动后复跑了本地全量回归(39 文件/424 用例、9 包 typecheck 全绿)。

## 1. 执行方式

前一轮尝试以 5 个并行子代理分派,因并发上限部分被取消(其残留改动已由后续执行核对或清理)。本轮有效执行:

| 子任务 | 范围 | 修改文件 | 包内测试结果 |
| --- | --- | --- | --- |
| A(01+06) | provider-apple 分页与数据完整性 | src/provider.ts、src/schemas.ts、test/provider.test.ts | 83 用例通过(基线 63 + 新增 20),typecheck 通过 |
| B(02) | importers plist DOCTYPE 接受面 | src/apple-xml.ts、test/apple-xml.test.ts、test/fixtures.ts | 4 文件 68 用例通过,typecheck 通过(修复了 3 处 TS18047) |
| C(08+07) | server TTL 与跨源导出头 | src/jobs.ts、src/app.ts、test/jobs.test.ts、test/cors-export.test.ts(新增) | 8 文件 72 用例通过(基线 66 + 新增 6),typecheck 通过 |
| D(05) | web Service Worker | public/sw.js、src/sw.test.ts | 3 文件 25 用例通过(sw 2→10),typecheck 通过 |
| E(03+04+09) | Docker/Compose/CI | Dockerfile、docker-compose.yml、.env.example、README.md、scripts/docker-smoke.mjs、.github/workflows/{ci,docker}.yml | node --check 通过;纯逻辑单测通过;YAML 结构断言通过;server build 通过;本机运行时矩阵 4 场景验证 |

任务单第 1 节的既有未提交改动(apps/web/src/App.*、api.*,packages/exporters/**,docs/verification/ 三份审查报告)未被覆盖。importers 的部分改动来自第一轮被取消的执行,由本轮 B 核对任务单要求后保留并补全;汇总提交时需按第 4 节逐文件确认归属。未执行任何 git commit/push/reset/clean。

## 2. 逐项修复与证据

### 01 / P1 Apple 相对分页 next —— 已修复
- `parseNextUrl(next, tracksPath)`:`new URL(next, API_ORIGIN)` 解析,相对路径(`/v1/catalog/...`)基于固定 origin,绝对 URL 兼容;保留 HTTPS、禁 userinfo、禁端口、host 大小写不敏感校验;路径检查升级为与当前 storefront+歌单的 `/tracks` 路径**精确相等**。
- `fetchAllPages` 计算一次 `tracksPath`(playlistId 经 encodeURIComponent)传入;redirect:'error'、分页预算、重复游标 guard、`allowedTerminalPolicies: ['next-absent']` 全部未动。
- 官方依据:fetching-resources-by-page(next 可为相对路径)。Apple 在线 API 仍标记 Preview;合成测试不等于真实 Token 实测。
- 测试:相对 next 11 页读 1005 首(超过 1000,逐页断言解析后绝对 URL)、绝对/相对混用、相对游标停滞、it.each 9 例投毒 next(协议相对外域、http 明文、:8443、user:pass@、不同 storefront、不同歌单、非 tracks 路径、library 路径、垃圾字符串,全部 PROVIDER_SCHEMA_DRIFT 且仅 1 次请求)。

### 02 / P1 标准 plist DOCTYPE —— 已修复
- 接受面:仅文档 prolog 中至多一条、逐字符受控的 `<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">`(FPI 与系统标识符精确匹配,仅 token 间空白与引号风格容错),整体跳过,不读 DTD、不解析外部实体、零网络。
- 继续拒绝:内部子集(含参数实体/外部实体/空子集)、非官方 FPI 或 URL(含 https://、file:// 变体)、SYSTEM 形式、非 plist 根名、畸形声明(8 类)、重复或位于中段/根后的声明,报 `IMPORT_XML_DOCTYPE_FORBIDDEN`。
- 未换解析库,零新依赖。既有"DOCTYPE 被拒"断言按新语义反转为"标准声明被跳过"(唯一语义反转,已在测试中注明);新增 DOCTYPE 接受面 10 个测试;带声明与无声明导入结果一致(顺序、重复曲目、缺失引用占位不变);fetch 注入监视确认全程零网络请求。

### 03 / P1 Docker 安装层缺工作区清单 —— 已修复,容器构建实测通过
- 按 pnpm-lock.yaml importers 补齐全部 10 个 workspace 清单:新增 apps/desktop、packages/importers、provider-apple、provider-qq 的 package.json 复制。
- desktop 取舍:纳入安装阶段(frozen-lockfile 要求全部 importer 在场),排除出运行镜像(@tauri-apps/cli 仅落 build 阶段;运行阶段不复制 node_modules、不执行 tauri build)。
- 保持 --frozen-lockfile、多阶段、非 root、最小运行镜像。
- **实测追加 1(容器专属,静态检查无法发现)**:pnpm 11 在容器/CI 等非交互环境下,依赖构建脚本未经批准会直接报 `ERR_PNPM_IGNORED_BUILDS`(exit 1),首次容器构建在 `pnpm install --frozen-lockfile` 步失败。pnpm 11 已移除 `onlyBuiltDependencies` 设置,改为 `allowBuilds` 映射;在 pnpm-workspace.yaml 显式声明 `allowBuilds: { esbuild: true }` 后,本地 `CI=1 pnpm install --frozen-lockfile` exit 0 且 lockfile 不变,容器内小实验确认 postinstall 获批执行。依据:pnpm approve-builds 文档(allowBuilds 写入 pnpm-workspace.yaml)、pnpm 11.0 release notes(收紧默认并替换该设置)。
- **实测追加 2(容器专属)**:运行镜像内 `EACCES: permission denied, open '/app/dist/index.js'`——构建阶段 esbuild 以 0660(root:root)写出 bundle,`COPY --from` 原样保留权限,非 root 运行用户(10001)不可读。先试 `COPY --chmod=0644` 失败(目录也被置 0644,丢失遍历位,`Cannot find module`),最终方案为运行阶段 USER 切换前 `RUN chmod -R a+rX /app`(文件补读、仅目录补执行位,属主保持 root)。

### 04 / P1 Compose Origin 白名单 —— 已修复,容器实跑验证
- config.ts 确认 ALLOWED_ORIGINS 为逗号分隔完整 origin、精确匹配、显式拒 `*`。compose 默认服务显式传 `ALLOWED_ORIGINS: http://127.0.0.1:${PORT:-4319},http://localhost:${PORT:-4319}`;app-lan 服务用 `${ALLOWED_ORIGINS:-...4319 回退}` 且文档要求 LAN 必须显式覆盖为实际 UI origin。未用 `*`,未绕过检查(实测 `*` 会被服务端启动期拒绝)。刻意只用单层插值(嵌套插值未经真实 docker compose 验证)。
- .env.example、README、compose 头部注释同步。
- 本机运行时矩阵(直接跑 dist/index.js,compose 同款环境):显式 ALLOWED_ORIGINS 时 7 项检查全过;不传时正确令牌+本机 origin 仍 403(复现原始缺陷);`ALLOWED_ORIGINS=*` 启动被拒;非回环缺令牌启动被拒。
- **容器实跑(192.168.1.49)**:`docker compose config --quiet`(合成令牌)exit 0,单层插值经真实 compose-go 解析;加固参数(--read-only、tmpfs /tmp、--cap-drop ALL、no-new-privileges)起容器后,默认 4319 与自定义 4567 两个宿主端口的检查矩阵均全过(见第 3 节)。

### 05 / P1 Service Worker 旧 HTML 驻留 —— 已修复
- HTML 入口(network-first,离线回退缓存;`response.ok && type==='basic'` 才写缓存;服务端错误原样返回不回退);`/assets/`、`/icons/`、`/manifest.webmanifest` 保持 cache-first。
- 缓存版本 v1→v2,activate 删除非当前版本缓存;`/api/`、`/healthz`、非 GET、跨源、带凭证请求、/sw.js 更新请求均不拦截。
- skipWaiting/clients.claim 保留并注明理由;注释如实说明浏览器对 sw.js 自身的更新检查不经过 fetch 拦截,本缺陷是入口策略与缓存失效不足。
- 测试 10 个:排除规则、旧入口→新部署联网拿新入口并刷新缓存、断网回退、hash 资源 cache-first、版本清理、立即接管等;以 vm + 受控 mock 驱动真实 sw.js 源码。

### 06 / P2 Apple 缺失 data 误判为空歌单 —— 已修复
- `appleCatalogPlaylistSchema.relationships` 改必填;`applePlaylistTracksSchema.data` 与续页信封 `data` 改必填数组(移除 nullish/transform)。首屏 relationships/tracks/data 缺失、null、非数组,续页 data 缺失/null,全部 PROVIDER_SCHEMA_DRIFT;仅真实 `data: []` 是合法空页(空歌单测试不回归)。
- 不完整结果不能导出由既有链路保证:complete=false 时 fetchAllTracks 抛 INCOMPLETE_PAGINATION,导出路由对 complete=false 返回 422 INCOMPLETE_PLAYLIST(server 侧 cors-export.test.ts 亦覆盖跨 TTL 后 404/409 路径)。

### 07 / P2 跨源下载读不到文件名 —— 已修复,含真实浏览器实测
- 服务端(app.ts 导出路由):对该响应显式补 `access-control-allow-origin`(回显已通过 Origin+Bearer 校验的 origin)、`vary: Origin`、`access-control-expose-headers: Content-Disposition`;无通配符,校验未放宽。实证发现 Hono 4.13.5 对 raw Response 不合并中间件暂存头,故原响应连 ACAO 都缺失,已在修复中补齐;OPTIONS 预检与实际响应一致。
- 新增 cors-export.test.ts 4 用例 + jobs.test.ts 2 用例;负向验证:还原缺陷代码后 CORS 契约 3 条如预期失败。
- **真实浏览器双端口跨源实测(本次执行,证据)**:真实 server(dist 构建产物,HOST=127.0.0.1:4399,ALLOWED_ORIGINS=http://127.0.0.1:4398,合成令牌)+ 第二 origin 静态页(:4398)+ Playwright Chromium。平台上游经 startServer 的 fetchImpl 注入本地 stub(music.163.com 两个请求,零真实平台流量)。结果:**跨源 fetch 读到 `content-disposition: attachment; filename*=UTF-8''netease_%E8%B7%A8%E6%BA%90...txt`,解码为 `netease_跨源下载验证歌单_2026-09-05.txt`,响应体 90 字节为一曲一行 TXT,退出码 0/PASS**。错误令牌请求被浏览器 reject(错误响应不带 CORS 头,"失败即关闭"既有行为),服务端日志确认其到达并返回 401。注:access-control-expose-headers/allow-origin 本身不可被页面 JS 跨源读取,响应可读即为其正确的浏览器级证明。验证脚本保存于仓库外 scratch(C:/Users/屑穹/.zcode/workspace/default/verify-cors/verify-cors.mjs),未入库。

### 08 / P2 TTL 过期后仍可取结果 —— 已修复
- getCompletedResult 在 deleteIfExpired 删除条目后重新确认 Map 中记录有效,失效即返回 undefined,与 get/cancel 语义一致。
- 可控时钟(fakeClock 注入)覆盖过期前/恰好到期/过期后的 get 与 getCompletedResult;同一任务两次导出跨 TTL 边界:首次 200 生成文件,边界上 404 JOB_NOT_FOUND 无文件;调度器触发删除+重排+清扫;零真实 sleep。负向验证:缺陷版初版测试未抓到,调整断言顺序后缺陷版恰好 1 条失败、修复版全绿。

### 09 / P2 CI 缺运行时与桌面验证 —— 已修复(GitHub Actions 实跑未验证)
- docker.yml 新增 smoke job:buildx 构建 linux/amd64(load: true)→ 合成配置 `docker compose config --quiet`(不打印令牌)→ `node scripts/docker-smoke.mjs --skip-build --image playlist-exporter:smoke`。paths 增加 compose 与 smoke 脚本。
- docker-smoke.mjs 重写:容器/镜像管理与检查项分离,支持 --image/--skip-build/--base-url/--token;检查矩阵 = healthz、无令牌 401、错令牌 401、非法 origin 403、未知 provider→400 INVALID_REQUEST、非法歌单输入→400 INVALID_PLAYLIST_INPUT、localhost origin 变体;全部合成输入,零真实凭证、零真实平台请求;docker 缺失时明确报错退出码 1。
- action SHA:11 个引用逐个经 GitHub API tag→commit 解引用核对;**发现并修复 ci.yml 中 actions/cache 的错误 SHA**(原引用在上游不存在,会导致 verify/e2e 缓存步红灯),已改为核实的 v4.2.0 真实 commit。
- Node 矩阵证据:package.json engines.node ">=24"、packageManager pnpm@11.19.0;ci.yml node:[22,24](22 为兼容性探测)。实证:pnpm 11.25.0 对 engines 不满足仅 WARN、install 成功 → pnpm 不会拦 Node 22;Node 22 上代码是否可用**无证据,不宣称**。
- 桌面构建保持 Planned,未执行,不声称安装包可用。未推送、未创建发布、未上传产物。

## 3. 最终回归(本次实际执行)

| 命令 | 结果 |
| --- | --- |
| `pnpm test` | 39 个测试文件、424 用例全部通过(基线 38/380,本轮净增 44;Docker 追加修复后复跑同数) |
| `pnpm typecheck` | 9 个包全部 Done,退出码 0 |
| `pnpm --filter @playlist-exporter/web build` | 成功(dist 317.15 kB JS / 3.96 kB CSS) |
| `pnpm --filter @playlist-exporter/server build` | 成功(dist/index.js 941.4 kB) |
| `pnpm --filter @playlist-exporter/web e2e` | 5/5 通过 |
| `git diff --check` | 通过(仅 core.autocrlf 的 LF→CRLF 提示,非空白错误) |
| `CI=1 pnpm install --frozen-lockfile` | exit 0,lockfile 无变更(容器/CI 环境下的安装可复现) |

**Docker 三连(192.168.1.49 实际执行,全部通过)**:

| 命令 | 结果 |
| --- | --- |
| `docker compose config --quiet`(合成令牌) | exit 0 |
| `docker buildx build --platform linux/amd64 -t playlist-exporter:amd64 . --load` | exit 0(镜像 sha256:8d3b29b1…;首次构建曾因第 2 节 03 项"实测追加"的 2 个缺陷失败,修复后通过) |
| `node scripts/docker-smoke.mjs --skip-build --image playlist-exporter:amd64`(默认模式,脚本自管容器生命周期) | exit 0:随机空闲端口 39463 与默认 4319 两组容器,各 7 项检查全过(healthz 200、无令牌 401、错令牌 401、非法 Origin 403、未知 provider→400 INVALID_REQUEST、非法歌单输入→400 INVALID_PLAYLIST_INPUT、loopback 变体接受),脚本自行清理容器;另以加固参数(--read-only/tmpfs/cap-drop/no-new-privileges)手工起容器对 4319 与 4567 两个宿主端口复跑了同一矩阵(--base-url 模式),均 exit 0 |

Docker 宿主环境:fnOS,Docker 28.5.2、buildx v0.29.1、Compose v2.40.3,x86_64。基础镜像 node:24-alpine 因 fnOS 镜像加速器(docker.fnnas.com)401 改经 docker.1ms.run 中转拉取并重打标签(镜像内容不变,Dockerfile 未改)。实测后 NAS 已清理临时工作目录与 node 基础镜像,保留 `playlist-exporter:amd64` 供部署。

## 4. 未验证项与边界(如实记录)

- **arm64 镜像**:未构建(仅 buildx 声明了 amd64;按任务单,arm64 只有实际执行成功才记通过)。
- macOS、Android、Tauri 桌面构建:未执行,无工具/未授权;桌面保持 Planned。
- 真实 Apple Developer Token、真实平台歌单导出:未触碰(边界要求);apple-music provider 仍为 Preview 口径。
- `pnpm audit`:本轮未执行(前轮因依赖清单外发被拒,未获授权不绕过)。
- Node 22 实际运行:无证据,ci.yml 中保留为探测腿。
- GitHub Actions 实际运行:workflow 已更新但未推送,远端 CI 未跑过。
- 浏览器级 Service Worker 更新流程的实机验证:未做(策略由 sw.test.ts 10 项单测覆盖,e2e 屏蔽 SW)。
- 生产依赖清单、锁文件未改动;未新增任何 npm 依赖。
- 测试全部为合成/脱敏数据;未调用真实用户歌单;错误详情与日志无凭证;未在 UI 直接访问平台接口;smoke 全程合成令牌。
- 凭据处理:用户的 NAS SSH 凭据仅在本次会话内经环境变量使用,未写入任何仓库文件、日志或文档;远端临时目录已删除。

## 5. 遗留与建议

1. ~~Docker 三连待远程宿主执行后回填本文件~~ 已完成(2026-09-05,192.168.1.49,见第 3 节);剩余 arm64 与 GitHub Actions 实跑待执行。
2. 缓存版本仍为手工递增(sw.js 为 public 静态文件);发布流程需约定"每次前端发布递增 CACHE_VERSION"。
3. `/api/*` 的 401/403/404 错误响应同样不带 ACAO(失败即关闭),本次未改动;若未来浏览器需要读取错误详情,需单独评估。
4. 汇总提交时按文件归属拆分:前一代理改动(apps/web App/api、exporters)、本轮各修复、docs,勿混入单一提交;未获确认不推送。
5. pnpm 11 的 `allowBuilds` 语义与 pnpm 10 的 `onlyBuiltDependencies` 不同;若未来降级或混用 pnpm 版本,需重审该配置(CI 矩阵中的 Node 22 探测腿也会覆盖此路径)。

## 6. 独立验收后的修复轮(F1–F4,2026-09-05)

输入:`docs/verification/INDEPENDENT-ACCEPTANCE-2026-09-05.md`(独立 GPT 审查,判定"需修复后合并",新发现 1 个 P1 + 3 个 P2)。修复执行方式:4 个并行子任务按文件归属切分,每项均"先写测试在旧实现上变红,再修复转绿"。审查者的边界(不真实平台请求、不以真实容器误删验证反例、不大规模重写、保留 A/B/C 归属)全部遵守。

### F1/P1 烟测误删非自有容器 → 已修复
- `scripts/docker-smoke.mjs`:删除固定容器名,改为唯一名(`playlist-exporter-smoke-<8hex>`);`docker run -d` 成功后立即从 stdout 登记容器 ID,清理只按 ID 执行 `docker rm -f <id>`(run 失败=零登记,健康失败=仍清自己);runDocker 失败详情新增 `redactDockerArgs` 脱敏(`ACCESS_TOKEN`/`APPLE_DEVELOPER_TOKEN` 值 → `[REDACTED]`,覆盖审查第 4 节 :295 打印合成令牌的问题);runMain 依赖注入化(spawnSync/fetch/probePort 可 mock)。检查矩阵、双端口场景、--base-url 模式、退出码语义不变。
- 回归:`scripts/docker-smoke.test.mjs` 新增 7 用例(冲突清零/只清自身 ID/健康失败仍清/清理失败告警/4319 占用跳过/脱敏),旧实现上 6 红 1 过。
- **真实 Docker 终验(192.168.1.49)**:用 F1–F4 后源码重建镜像(BUILD_EXIT=0)后,新版 smoke 默认模式 SMOKE_EXIT=0,日志可见唯一名(`playlist-exporter-smoke-575a73b9`)、ID 登记、`rm -f <id>` 清理,双场景 7 项检查全过,零残留容器。

### F2/P2 SW 缓存写失败误判网络失败 → 已修复
- `apps/web/public/sw.js`:缓存写入(caches.open/put)包成尽力而为,写失败仅 console.debug;入口 network-first 只在 `fetch` 真正 reject 时走离线回退(无缓存继续抛原始错误);hash 资源 cache-first 未命中联网取回不再因写失败丢响应。排除规则与 CACHE_VERSION 语义不变。
- 回归:`apps/web/src/sw.test.ts` 新增 4 场景(审查者 QuotaExceeded 探针场景改为断言 NEW、无旧缓存+写失败、hash 资源写失败、断网回退不回归),旧实现上 4 红;审查者内存 VM 探针改前 `OLD` → 改后 `NEW`。
- **真实浏览器 e2e(新增 `apps/web/e2e/sw-update.e2e.ts`,serviceWorkers: 'allow')**:v1 缓存激活清理、入口 network-first(context.route 拦截)、断网回退,3/3 通过;全量 e2e 8/8。

### F3/P2 DOCTYPE 前导未贯通统一入口 → 已修复
- `packages/importers/src/index.ts`:新增有界 prolog 扫描(空白/`<?xml?>`/注释,4096 字符上限),首元素为 `<plist` 或 `<!DOCTYPE`(不分大小写)即路由到既有严格解析器;入口不做 DOCTYPE 内容判断,接受/拒绝仍由 apple-xml.ts 裁决。json/text/unknown 路由不变。
- 回归:`packages/importers/test/import-file.test.ts` 新增 7 用例(DOCTYPE-first 合法库导入、声明/注释组合、内部子集与未知 DTD 报 IMPORT_XML_DOCTYPE_FORBIDDEN 而非 UNKNOWN_FORMAT、小写 doctype、4096 上限守护),旧实现上 5 红;importers 全包 75/75 通过。

### F4/P2 PORT=80 派生 Origin 被拒 → 已修复
- `apps/server/src/config.ts`:parseOrigin 保留全部结构性拒绝(`*`/`null`/空串/userinfo/路径/search/hash/非 http(s)),移除 `url.origin !== candidate` 等值检查,统一返回规范 origin(`http://127.0.0.1:80` → `http://127.0.0.1`);defaultOrigins 同样规范化。不放宽任何边界。
- 回归:`apps/server/test/config.test.ts` 扩充 4 用例组(PORT=80/4319/4567、去重、大小写/尾斜杠规范化、拒绝面不变),旧实现上 4 红;server 全包 86/86 通过。
- 真实 compose 验证(192.168.1.49):`PORT=80 docker compose config` 插值 exit 0,产出 `http://127.0.0.1:80,http://localhost:80`(即 F4 修复后 config 接受并规范化的输入)。容器内监听 80 属非 root 无 CAP_NET_BIND_SERVICE 的另一话题,不在 F4 范围,如实注明。

### 审查附带小项
- Dockerfile 注释精度修正:a+rX 的 X 也会为已有任一执行位的文件补执行位(原注释"只对目录补执行位"不准确;本镜像产物无执行位,实际效果不变)。
- ci.yml:Node 22 从主阻塞矩阵拆出,改为独立 `node22-probe` job(ubuntu,continue-on-error: true),明示"非阻塞探测、失败不作为合并门槛"——响应审查"需明确这是支持要求还是非阻塞探测"。

### 修复轮回归(全部实际执行,退出码 0)
`pnpm test`:40 文件/456 用例(前轮 424 + F1–F4 新增 32);`pnpm typecheck` 9 包;web/server build;`pnpm --filter @playlist-exporter/web e2e` 8/8;`git diff --check` 通过。

### 仍未验证项
GitHub Actions 实跑(workflow 未推送);arm64;Tauri 桌面;真实 Apple Token;真实容器名称冲突场景(以 mock 覆盖,真实冲突需预置同名容器,违反审查者"不误删真实容器"边界,故不做);Node 22 实际运行(node22-probe 推送后才有数据)。
